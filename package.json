{
  "name": "backend-notes",
  "version": "0.0.1",
  "description": "This is the backend for notes project",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "start": "node index.js",
    "build:ui": "rm -rf dist && cd ../notes_frontend/ && npm run build && cp -r dist ../notes_fso",
    "dev": "nodemon index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Md. Nowshad Islam",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.21.2",
    "morgan": "^1.10.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.9"
  }
}
